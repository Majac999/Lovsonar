+  1 name: LovSonar v4.0 Weekly Compliance
+  2 
+  3 on:
+  4   schedule:
+  5     - cron: '0 7 * * 1'
+  6   workflow_dispatch:
+  7 
+  8 jobs:
+  9   run_compliance_system:
+ 10     runs-on: ubuntu-latest
+ 11     permissions:
+ 12       contents: write
+ 13 
+ 14     steps:
+ 15       - name: 1. Hent koden
+ 16         uses: actions/checkout@v4
+ 17 
+ 18       - name: 2. Sett opp Python
+ 19         uses: actions/setup-python@v5
+ 20         with:
+ 21           python-version: '3.11'
+ 22 
+ 23       - name: 3. Installer avhengigheter
+ 24         run: |
+ 25           python -m pip install --upgrade pip
+ 26           pip install -r requirements.txt
+ 27 
+ 28       - name: 4. Kjor LovSonar Analyse
+ 29         env:
+ 30           EMAIL_USER: ${{ secrets.EMAIL_USER }}
+ 31           EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
+ 32           EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
+ 33         run: python lovsonar_v4.py
+ 34 
+ 35       - name: 5. Lagre database
+ 36         run: |
+ 37           git config --global user.name "LovSonar Bot"
+ 38           git config --global user.email "bot@lovsonar.no"
+ 39           git add *.db *.json || true
+ 40           git commit -m "Oppdaterte ukentlig database [skip ci]" || exit 0
+ 41           git push
